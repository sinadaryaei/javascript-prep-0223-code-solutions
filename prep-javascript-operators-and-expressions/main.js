
const number1 = 7;
const number2 = 4;

const product = number1 * number2;

console.log('value of product:', number1 * number2);
console.log('value of product:', typeof product);

const charge = 8;
const payment = 10;

const amountRemaining = charge - payment;

console.log('value of amountRemaining:', amountRemaining);
console.log('value of amountRemaining:', typeof product);

const tests = 70;
const assignments = 90;
const final = 90;

const grade = (tests + assignments + final) / 3;

console.log('value of the grade:', (tests + assignments + final) / 3);
console.log('typeof grade:', typeof grade);

const firstName = 'Sina ';
const lastName = 'Daryaei';

const fullName = firstName + lastName;

console.log('value of fullName:', firstName + lastName);
console.log('type of fullName:', typeof fullName);

const pH = 10;

const isAcidic = true;

console.log('value of isAcidic:', pH < 7);
console.log('type of isAcidic:', typeof isAcidic);

const numberOfSoldiers = 100;

const isSparta = numberOfSoldiers === 300;

console.log('the value of isSparta:', isSparta);
console.log('typeof of isSparta:', typeof isSparta);

let nickname = fullName;

nickname += ' is the GOAT';

console.log('value of nickname:', nickname);
console.log('typeof nickname:', typeof nickname);
